# Dependency set for CI unit tests.
#
# The authoritative source tree lives at the repo-root `kazi/` package and is
# imported via pythonpath (see [tool.pytest.ini_options] in pyproject.toml) —
# NOT pip-installed, because installing the `kazi-*` packages would shadow the
# root tree with their re-export subsets. So CI installs only third-party deps.
#
# This is the minimal set that makes `pytest tests/unit/` pass on 3.11–3.13.

# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.0
hypothesis>=6.100

# Core runtime (LangGraph brain + checkpointer)
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0
langchain-core>=0.3.0
aiosqlite>=0.20.0

# LLM adapters exercised by unit tests
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.2.0

# Config / IO
pyyaml>=6.0
httpx>=0.27
sqlalchemy>=2.0

# Telemetry (unit tests assert tracer wiring)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
